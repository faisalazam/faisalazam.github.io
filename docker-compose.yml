services:
  jekyll:
    build: .
    container_name: jekyll-site
    ports:
      - "4000:4000"      # Jekyll's default port
      - "35729:35729"    # Live reload port
    volumes:
      - ./:/srv/jekyll